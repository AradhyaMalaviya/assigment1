# Browser-visible same-origin proxy base. Do not point this at the backend directly.
PUBLIC_API_BASE_URL=/api

# Server-only Hono backend origin, used by the SvelteKit proxy and later auth guard.
BACKEND_ORIGIN=http://localhost:3000
